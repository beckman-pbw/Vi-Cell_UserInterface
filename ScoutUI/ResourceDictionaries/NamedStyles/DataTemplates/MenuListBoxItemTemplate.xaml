<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="MenuListBoxItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="252" />
            </Grid.ColumnDefinitions>

            <Path Data="{Binding Image}" Stretch="Uniform" Width="37" Height="28" Grid.Column="1"
                  Fill="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem, Mode=FindAncestor}, Path=Foreground}" />

            <StackPanel Grid.Column="2" Margin="5">
                <TextBlock Text="{Binding Path=Title, FallbackValue=Title}" FontFamily="Segoe UI" FontSize="20" VerticalAlignment="Center"
                           Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem, Mode=FindAncestor}, Path=Foreground}" />
            </StackPanel>
        </Grid>

    </DataTemplate>

</ResourceDictionary>