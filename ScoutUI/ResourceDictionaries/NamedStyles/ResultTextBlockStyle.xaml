<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ResourceDictionaries/ConverterResources.xaml"/>
        <ResourceDictionary Source="/ResourceDictionaries/NamedStyles/SimpleStyles.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ResultTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlock}">
        <Setter Property="Margin" Value="5,0,0,0" />
        <Setter Property="Foreground" Value="{DynamicResource GenericVeryDark}" />
        <Setter Property="Background" Value="Transparent" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding ExecutionStatus, Converter={StaticResource ExecutionStatusToBooleanConverter}}" Value="True">
                <Setter Property="FontWeight" Value="Bold" />
            </DataTrigger>
            <DataTrigger Binding="{Binding ExecutionStatus, Converter={StaticResource ExecutionStatusToBooleanConverter}}" Value="False">
                <Setter Property="FontWeight" Value="Normal" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>