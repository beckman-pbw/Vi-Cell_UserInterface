namespace ScoutUtilities.Enums
{
   
    public enum Batch
    {
        Off = 0,
        //BioProcess = 1, // No longer needed but could be later if BioProcess gets added back to the project scope someday
        QualityControl = 2
    }
}